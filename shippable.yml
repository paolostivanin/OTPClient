language: c

branches:
  only:
    - master

compiler:
  - gcc
  - clang

# setup also libcotp and libbaseencode
build:
  ci:
    - sudo apt install cmake libgtk-3-dev libgcrypt20-dev libzip-dev
    - mkdir build && cd $_
    - cmake ../
    - make -j2
