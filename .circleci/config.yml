version: 2.0

jobs:
  ubuntuLatest:
    docker:
      - image: ubuntu:resolute
    steps:
      - checkout
      - run: apt update && DEBIAN_FRONTEND=noninteractive apt -y install git gcc clang cmake libgcrypt20-dev libgtk-4-dev libzip-dev libjansson-dev libpng-dev libzbar-dev libprotobuf-c-dev libsecret-1-dev uuid-dev libprotobuf-dev libqrencode-dev libadwaita-1-dev
      - run: chmod +x .ci/install_deps.sh && .ci/install_deps.sh
      - run: chmod +x .ci/install_otpclient.sh && .ci/install_otpclient.sh

  debianStable:
    docker:
      - image: debian:stable
    steps:
      - checkout
      - run: apt update && apt -y install git gcc clang cmake libgcrypt20-dev libgtk-4-dev libzip-dev libjansson-dev libpng-dev libzbar-dev libprotobuf-c-dev libsecret-1-dev uuid-dev libprotobuf-dev libqrencode-dev libadwaita-1-dev
      - run: chmod +x .ci/install_deps.sh && .ci/install_deps.sh
      - run: chmod +x .ci/install_otpclient.sh && .ci/install_otpclient.sh

  fedoraLatestStable:
    docker:
      - image: fedora:latest
    steps:
      - checkout
      - run: dnf -y update && dnf -y install git gcc clang cmake make libgcrypt-devel gtk4-devel libzip-devel jansson-devel libpng-devel zbar-devel protobuf-c-devel libsecret-devel libuuid-devel protobuf-devel qrencode-devel libadwaita-devel
      - run: chmod +x .ci/install_deps.sh && .ci/install_deps.sh
      - run: chmod +x .ci/install_otpclient.sh && .ci/install_otpclient.sh

  archlinux:
    docker:
      - image: archlinux:latest
    steps:
      - checkout
      - run: pacman -Syu --noconfirm && pacman -S --noconfirm pkg-config git gtk4 libgcrypt zbar gcc clang cmake make libzip jansson libpng protobuf-c libsecret util-linux-libs qrencode  	libadwaita
      - run: chmod +x .ci/install_deps.sh && .ci/install_deps.sh
      - run: chmod +x .ci/install_otpclient.sh && .ci/install_otpclient.sh

workflows:
  version: 2
  build:
    jobs:
      - ubuntuLatest
      - debianStable
      - fedoraLatestStable
      - archlinux
